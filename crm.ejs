<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CRM</title>
</head>
<body>
  <h1>CRM</h1>
  <ul>
    <% for(let i = 0; i < clients.length; i++){ %>
      <li>
        <a href="/crm/<%= clients[i]._id %>">
          <%= clients[i].first %>
          <%= clients[i].last %>
        </a>
      </li>
    <% } %>
  </ul>
  <a href='/crm/new'>Create New CRM Client</a>
</body>
</html>